packages:
  - "apps/*"
  - "packages/*"

# pnpm >=10 blocks dependency build scripts by default. esbuild (used by vite and
# vitest) needs its install script to set up its platform binary.
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
